server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/engseg
    username: engseg
    password: engseg123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: engseg-secret-key-2026-very-long-and-secure-key-for-jwt-token-authentication
  expiration: 86400000

spring.scheduling.enabled: true
